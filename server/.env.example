# 服务端口（前端构建产物也由它托管）
PORT=5173

# SQLite 数据库文件路径（默认 server/data/knowledge.db）
# DB_PATH=./data/knowledge.db

# —— AI 问答（预留接口，可选）——
# 配置后，检索未命中时按回车会调用 AI 实时生成回答。
# 兼容 OpenAI /chat/completions 接口。
# AI_API_KEY=sk-xxxx
# AI_BASE_URL=https://api.openai.com/v1
# AI_MODEL=gpt-4o-mini
